# Examples

Run a local simulation without touching external systems:

```sh
agentpermit check examples --format text
agentpermit explain examples
```

The example intentionally denies the outbound message action so humans can see
how AgentPermit explains approval-sensitive work.
